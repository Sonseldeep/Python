#to generate random integers we use module random using function called
# random.randint(lowerBound,UpperBound)

import random
 

random_number = random.randint(1, 100) #here 1 and 100 both are inclusive
 
print(random_number)
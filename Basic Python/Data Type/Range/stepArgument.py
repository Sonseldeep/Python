# The step Argument
# The complete syntax of the range() function is:

# numbers = range(start, end, step)
# By default, 
# the step argument is 1
# if it's not used.
# That's why we are getting numbers at intervals of 1.

numbers = list(range(1,10,2))
print(numbers)
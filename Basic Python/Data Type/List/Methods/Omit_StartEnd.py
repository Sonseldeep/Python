numbers = [10,20,30,40,50]

# If we omit the start index, 
# the slicing starts from the first item 
# (not the first index).
print(numbers[:4])

print(numbers[2:])

# What if we skip both start and end index?
print(numbers[:])
#  simply prints all item in list